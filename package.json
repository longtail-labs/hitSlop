{
  "private": true,
  "name": "hitslop-swift",
  "version": "0.1.4",
  "type": "module",
  "scripts": {
    "commit": "bun run scripts/commit.ts",
    "release": "bun run scripts/release.ts",
    "release:local": "bun run scripts/release.ts --local",
    "release:ci": "bun run scripts/release.ts --ci"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "bun-types": "^1.3.8",
    "@inquirer/prompts": "^8.0.0"
  }
}
